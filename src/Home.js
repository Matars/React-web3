import {
    Button,
    Alert,
    AlertIcon,
    AlertTitle,
    AlertDescription,
    Input,
    Box,
  } from "@chakra-ui/react";
import React, { useState } from "react";
import { Container, Heading } from "@chakra-ui/layout";

export const Home = () => {
    return <Box>Home</Box> 
}